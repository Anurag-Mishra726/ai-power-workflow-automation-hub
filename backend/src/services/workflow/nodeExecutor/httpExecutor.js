import { createExecutionResult } from "../../../utils/executionResult.js";

export const httpExecutor = async ({data, nodeId, context}) => {
    try {
        const result = await fetch("")
    } catch (error) {
        
    }
}

// implement the fetch here by axios and also stop the run when the error is there using non-retryable.